# CsScriptTest
Example for CS-Script version 4.14.11 (using the nuget package) for C# and .net 10.0.

I had to change the sample code to indicate how to use namespace (for interface) and what generated code looks like in debugger.

This example shows how to compile code in an assembly and used this assembly in a script that is loaded.
